# Running GPU Neural Networks:

`make run-gpu`

# Running CPU Neural Networks:

`make densecpu`, `make run-cpu`

# Dataset:

The MNIST dataset that we used in our experiments is included in the zip; however, you can also run `make mnist` to pull the needed data.